<UserControl x:Class="District_3_App.Settings_Privacy_GUI.SettingsPrivacy_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:District_3_App.Settings_Privacy_GUI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">
    <Grid x:Name="settingsPrivacyGrid" HorizontalAlignment="Center" VerticalAlignment="Center">
        <ListView x:Name="groupsListView" 
              d:ItemsSource="{d:SampleData ItemCount=5}" 
              Width="200" 
              Margin="300,333,300,45" 
              BorderThickness="0">
            <!-- Define a style for ListViewItem to change the selected item color -->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <!-- Trigger to change the background color of the selected item -->
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="LightBlue"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <!-- Your ListView content -->
        </ListView>

        <ListView x:Name="restrictedPostsAudienceListView" 
      HorizontalAlignment="Center" 
      Height="50" 
      Margin="0,173,0,0" 
      VerticalAlignment="Top" 
      Width="200" 
      d:ItemsSource="{d:SampleData ItemCount=5}" 
      BorderThickness="0">
            <!-- Define a style for ListViewItem to change the selected item color -->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <!-- Trigger to change the background color of the selected item -->
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="LightBlue"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <!-- You can define columns here -->
                    <GridViewColumn Header="Column Header" Width="Auto"/>
                </GridView>
            </ListView.View>
            <!-- Remove any border on the ListView -->
            <ListView.Template>
                <ControlTemplate TargetType="ListView">
                    <Border BorderThickness="0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </ListView.Template>
        </ListView>

        <Label Content="Restricted Posts Audience:" FontSize="10" HorizontalAlignment="Left" Margin="300,145,0,0" VerticalAlignment="Top"/>
        <Label Content="Groups:" FontSize="10" HorizontalAlignment="Left" Margin="300,310,0,0" VerticalAlignment="Top" Height="23" Width="63"/>
        <Label Content="Restricted Stories Audience:" FontSize="10" HorizontalAlignment="Left" Margin="296,73,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.5,0.5" Height="23" Width="133">
            <Label.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="-0.316"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Label.RenderTransform>
        </Label>
        <ListView x:Name="restrictedStoriesAudienceListView" 
      d:ItemsSource="{d:SampleData ItemCount=5}" 
      Height="50" 
      Width="200" 
      Margin="300,95,300,305"
      ScrollViewer.VerticalScrollBarVisibility="Auto">
            <!-- Define a style for ListViewItem to change the selected item color -->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <Border Background="{TemplateBinding Background}">
                                    <ContentPresenter/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <!-- Trigger to change the background color of the selected item -->
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="LightBlue"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <!-- You can define columns here -->
                    <GridViewColumn Header="Column Header" Width="Auto"/>
                </GridView>
            </ListView.View>
            <!-- Remove any border on the ListView -->
            <ListView.Template>
                <ControlTemplate TargetType="ListView">
                    <Border BorderThickness="0">
                        <ScrollViewer>
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </ListView.Template>
        </ListView>


        <Label Content="Settings &amp; Privacy" FontSize="17" HorizontalAlignment="Center" Margin="0,15,0,0" VerticalAlignment="Top" Height="38" Width="148"/>
        <Label Content="Password:" FontSize="10" HorizontalAlignment="Left" Margin="294,53,0,0" VerticalAlignment="Top" Width="63" Height="26"/>
        <TextBox x:Name="passwordTextBox" HorizontalAlignment="Left" Margin="362,53,0,0" TextWrapping="Wrap" Text="*********" VerticalAlignment="Top" Width="120"/>
        <Label Content="Blocked Accounts:" FontSize="10" HorizontalAlignment="Left" Margin="300,228,0,0" VerticalAlignment="Top" Height="22" Width="109"/>
        <ListView Width="200" 
      Height="50" 
      x:Name="blockedProfilesListView" 
      d:ItemsSource="{d:SampleData ItemCount=5}" 
      Margin="300,255,300,145">
            <ListView.Resources>
                <!-- Define a style for ListViewItem to remove border -->
                <Style TargetType="ListViewItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>

                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <!-- You can define columns here -->
                    <GridViewColumn Header="Column Header" Width="Auto"/>
                </GridView>
            </ListView.View>
            <!-- Remove any border on the ListView -->
            <ListView.Template>
                <ControlTemplate TargetType="ListView">
                    <Border BorderThickness="0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </ListView.Template>
        </ListView>

        <Border CornerRadius="10" Height="30" Width="168" Padding="5" Background="LightGray" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="338,0,0,10">
            <CheckBox x:Name="isProfilePrivateCheckBox" Content="Public / Private Account"/>
        </Border>

        <Button Content="Add" FontSize="11" x:Name="addRestrictedStoriesUserButton" HorizontalAlignment="Left" Margin="499,76,0,0" VerticalAlignment="Top" Background="White"/>
        <Button Content="Add" FontSize="11" x:Name="addRestrictedPostsUserButton" HorizontalAlignment="Left" Margin="499,150,0,0" VerticalAlignment="Top" Background="White" RenderTransformOrigin="0.565,0.537"/>

        <Button Content="Add" FontSize="11" x:Name="addBlockedAccountButton" HorizontalAlignment="Left" Margin="499,228,0,0" VerticalAlignment="Top" Background="White"/>
        <Button Click="mantainGroupsButton_Click" Content="Edit" FontSize="11" x:Name="mantainGroupsButton" HorizontalAlignment="Left" Margin="500,312,0,0" VerticalAlignment="Top" Background="White" RenderTransformOrigin="0.5,0.5"/>



        <Button Content="Remove" FontSize="11" x:Name="removeRestrictedStoriesUserButton" HorizontalAlignment="Left" Margin="537,76,0,0" VerticalAlignment="Top" Background="White"/>
        <Button Content="Remove" FontSize="11" x:Name="removeRestrictedPostsUserButton" HorizontalAlignment="Left" Margin="537,149,0,0" VerticalAlignment="Top" Background="White" RenderTransformOrigin="0.565,0.537"/>

        <Button Content="Remove" FontSize="11" x:Name="removeBlockedAccountButton" HorizontalAlignment="Left" Margin="537,228,0,0" VerticalAlignment="Top" Background="White"/>
        <Button Click="leaveGroupButton_Click" Content="Leave" FontSize="11" x:Name="leaveGroupButton" HorizontalAlignment="Left" Margin="537,312,0,0" VerticalAlignment="Top" Background="White" RenderTransformOrigin="0.565,0.537"/>
        <TextBox x:Name="testSelectedItem" HorizontalAlignment="Left" Margin="670,333,0,0" TextWrapping="Wrap" Text="Selected group name" VerticalAlignment="Top" Width="120"/>


    </Grid>
</UserControl>
